# AT2026RC67 onboard stack — Raspberry Pi 5 (8 GB), Raspberry Pi OS Bookworm 64-bit
#
# apt (system) packages — install these first:
#   sudo apt install -y python3-opencv python3-picamera2 python3-numpy libzbar0
#
# pip packages (use --break-system-packages on Bookworm, or a venv with
# --system-site-packages so picamera2 stays visible):
pyzbar>=0.1.9          # QR fallback decoder (needs libzbar0 above)
pymavlink>=2.4.40      # Pixhawk 2.4.8 link over /dev/serial0
rpi-hardware-pwm>=0.2  # MG90S winch on hardware PWM (GPIO12)
# vl53l7cx             # ST ULD binding for the 3x ToF array (vendor wheel)
